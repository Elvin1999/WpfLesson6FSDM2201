<Window x:Class="Binding.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Binding"
        mc:Ignorable="d"
        Title="InfoWindow" Height="450" Width="800">
    <Grid>
        <!--<StackPanel>
            <Label>Vendor</Label>
            <TextBox Text="{Binding InfoCar.Model,UpdateSourceTrigger=PropertyChanged}" FontSize="30" ></TextBox>
            <Label>Model</Label>
            <TextBox Text="{Binding InfoCar.Vendor,UpdateSourceTrigger=PropertyChanged}" FontSize="30" ></TextBox>
            <Label>Year</Label>
            <TextBox Text="{Binding InfoCar.Year,UpdateSourceTrigger=PropertyChanged}" FontSize="30" ></TextBox>
        </StackPanel>-->
        <StackPanel>
            <Label>Vendor</Label>
            <TextBox Text="{Binding Car.Model,UpdateSourceTrigger=PropertyChanged}" FontSize="30" ></TextBox>
            <Label>Model</Label>
            <TextBox Text="{Binding Car.Vendor,UpdateSourceTrigger=PropertyChanged}" FontSize="30" ></TextBox>
            <Label>Year</Label>
            <TextBox Text="{Binding Car.Year,UpdateSourceTrigger=PropertyChanged}" FontSize="30" ></TextBox>
        </StackPanel>
    </Grid>
</Window>
